{
  "private": true,
  "scripts": {
    "test": "mocha --recursive",
    "test:watch": "mocha --recursive --watch --reporter progress"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.1"
  }
}